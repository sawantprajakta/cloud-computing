version: '2'
#first services
services:
#name of that service
  numpyapp-services:
#service properties
#build for image
    build: ./numpyapp
#application volume running dir
    volumes:
      - ./numpyapp:/usr/src/app
